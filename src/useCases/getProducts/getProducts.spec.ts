(function () {
describe('Get Products Controller', () => {
    it('should get ', () => {
        const result = true;
        expect(result).toBe(true);
    });
});

describe('Get DTO Controller', () => {
    it('should test that true === true', () => {
        const result = true;
        expect(result).toBe(true);
    });
});

describe('Get Use Case', () => {
    it('should test that true === true', () => {
        const result = true;
        expect(result).toBe(true);
    });
});
})();
